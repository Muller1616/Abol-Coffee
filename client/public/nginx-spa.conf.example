# Example nginx SPA config for Abol Coffee client/dist.
# Copy into your host nginx site and point root at the built assets.
#
# server {
#   listen 443 ssl http2;
#   server_name yourdomain.com;
#   root /var/www/abol-coffee/client/dist;
#   index index.html;
#
#   location / {
#     try_files $uri $uri/ /index.html;
#   }
#
#   # Same-origin API reverse proxy (recommended for cookie auth):
#   location /api/ {
#     proxy_pass http://127.0.0.1:4001/api/;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
# }

# Abol Coffee Client

React + Vite frontend for the Restaurant QR Digital Menu.

See the root [README](../README.md) for setup, env vars, and product rules.

```bash
cp .env.example .env
npm install
npm run dev
```

Leave `VITE_API_URL` empty locally so cookie auth uses the Vite proxy to the API.
